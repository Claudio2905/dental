# Production environment configuration
# A�adimos valores por defecto para evitar errores cuando las variables no est�n definidas
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:mysql://localhost:3306/dental_db}
spring.datasource.username=${JDBC_DATABASE_USERNAME:root}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:admin}
spring.jpa.hibernate.ddl-auto=update

# Auto-detect database type
spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.MySQLDialect}
